Pull

git pull is used to fetch and merge the latest changes from a remote repository into your current local branch.

It is commonly used to keep your local project up to date with changes made by others.

Under the hood, git pull runs:

git fetch

followed by git merge

Source:
https://git-scm.com/docs/git-pull
